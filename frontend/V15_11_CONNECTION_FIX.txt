V15.11 frontend connection fix

The dashboard now shows Connected when either the compact live `/status` payload or the broker-backed `/banking-status` payload succeeds. A slow or failed `/reports` request no longer leaves the entire dashboard stuck on Connecting.
