V16.2 Fast Intelligence Engine Loading + AI Portfolio UI

Changes:
- Engine results now appear progressively as each endpoint answers.
- CEO, Board, Memory, Scientist and V16 Portfolio endpoints load first.
- Five-request worker pool prevents a browser/CORS/database request storm.
- Normal endpoint timeout reduced to 12 seconds; known composite endpoints use 20 seconds.
- Initial failures retry once quietly in the background after the page is usable.
- The page no longer waits for all endpoints before updating engine counts.
- Adds the visible AI PORTFOLIO tab using /v16/portfolio/status.

Frontend-only deployment. Replace/deploy the complete frontend folder.
