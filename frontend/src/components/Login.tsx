export function Login({ username, password, error, setUsername, setPassword, onLogin }: { username: string; password: string; error: string; setUsername: (value: string) => void; setPassword: (value: string) => void; onLogin: () => void }) {
  return <div className="app"><h1>TradeBot Secure Login</h1><div className="card" style={{ maxWidth: 520, margin: "40px auto" }}><h2>Login</h2><p className="muted">Enter your admin username and password.</p><input value={username} onChange={(e) => setUsername(e.target.value)} placeholder="Username" style={{ width: "100%", padding: 14, borderRadius: 12, marginBottom: 12 }} /><input type="password" value={password} onChange={(e) => setPassword(e.target.value)} placeholder="Password" style={{ width: "100%", padding: 14, borderRadius: 12, marginBottom: 12 }} onKeyDown={(e) => e.key === "Enter" && onLogin()} /><button onClick={onLogin}>Login</button>{error && <p className="loss">{error}</p>}</div></div>;
}
