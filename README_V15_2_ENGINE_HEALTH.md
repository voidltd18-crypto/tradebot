# TradeBot V15.2 Exact Engine Health

Adds:

- `GET /v15/operations/engine-health`
- fault-isolated component and route diagnostics
- exact offline reasons instead of only a total count
- V13 Memory market extraction fix (`firstObject` Python typo removed)

Deploy by replacing the existing `backend` folder.
